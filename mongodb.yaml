apiVersion: everest.percona.com/v1alpha1
kind: DatabaseCluster
metadata:
  name: mongodb-basic
  namespace: everest
spec:
  backup:
    enabled: true
    # pitr:
    #   backupStorageName: s3
    #   enabled: true
    # schedules:
    # - backupStorageName: s3
    #   enabled: true
    #   name: backup-r33
    #   schedule: 30 * * * *
  engine:
    replicas: 3
    resources:
      cpu: "1"
      memory: 4G
    storage:
      class: standard-rwo
      size: 25Gi
    type: psmdb
    version: 7.0.14-8
